# Render deployment configuration
# DISABLED: This project is deployed on DigitalOcean, not Render
# 
# To completely remove Render:
# 1. Go to https://dashboard.render.com
# 2. Find your "autoppia-miner" service
# 3. Click "Settings" → "Delete Service"
#
# This file exists only to prevent build errors if Render is still connected.
# You can safely delete this file after removing the Render service.

services:
  - type: web
    name: autoppia-miner-api
    env: python
    # Disabled - using DigitalOcean instead
    buildCommand: echo "This service is disabled. Using DigitalOcean deployment."
    startCommand: echo "This service is disabled. Using DigitalOcean deployment."
    envVars:
      - key: DISABLED
        value: "true"
    # Disable auto-deploy
    autoDeploy: false
